version: '3'

name: my-ssh-xpra
services:
  sandbox:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    working_dir: /app
    command: bash start.sh
    ports:
      - 2222:22
      - 80:80
    environment:
      - DISPLAY=:10
    restart: always
